# Required on Render (Environment → Environment Variables)
# Use your Atlas connection string — database name is added automatically if missing
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/shopco?retryWrites=true&w=majority
JWT_SECRET=your-long-random-secret

# Optional
PORT=4000
NODE_ENV=production
